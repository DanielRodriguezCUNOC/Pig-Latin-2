package com.piglatin.common.application.dto;

/**
 * This enum is used for NodeDTO
 * With this enum we can identifier if the node is a file or directory
 */
public enum NodeType {
    FILE,
    DIRECTORY
}
