package com.piglatin.common.application.dto;

/**
 * This enum is used by CompileRequestDTO for identifier the mode of compilation
 */
public enum CompilationMode {
    VALIDATE_ONLY,
    C3D_ONLY,
    C_CODE_ONLY,
    FULL
}
